{
  "name": "clear-streaming-history",
  "version": "1.0.0",
  "description": "Chrome extension to bulk-clear watch history on supported streaming services.",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": ["**/tests/**/*.test.js"]
  }
}
